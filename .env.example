# Database
# Para desenvolvimento local, use SQLite:
# DATABASE_URL="file:./dev.db"

# Para produção, use PostgreSQL (ex: NeonDB, Supabase, etc):
DATABASE_URL="postgresql://user:password@host:5432/database?sslmode=require"

# Opcional: Para desenvolvimento
# NODE_ENV="development"

# ============================================
# Autenticação no Liferay (Opcional)
# ============================================
# Configure apenas se os ambientes GREEN/BLUE exigem login
# Veja: LIFERAY_AUTH.md para documentação completa

# URL da página de login
LIFERAY_LOGIN_URL=https://seu-liferay-stg.com/c/portal/login

# Credenciais de uma conta de serviço (SEM MFA!)
LIFERAY_USER=service.account@empresa.com
LIFERAY_PASS=sua-senha-segura

# Seletores CSS (opcional - usa defaults se não informado)
# LIFERAY_USER_SELECTOR=input[name="login"]
# LIFERAY_PASS_SELECTOR=input[name="password"]
# LIFERAY_SUBMIT_SELECTOR=button[type="submit"]

# Vercel (definido automaticamente na plataforma)
# VERCEL=1
